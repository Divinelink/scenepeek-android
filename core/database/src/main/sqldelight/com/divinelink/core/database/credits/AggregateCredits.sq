CREATE TABLE IF NOT EXISTS aggregateCredits (
    id INTEGER PRIMARY KEY
);

checkIfExist:
SELECT EXISTS(SELECT 1 FROM aggregateCredits WHERE id = ?);

insert:
INSERT INTO aggregateCredits (id)
VALUES (?);
